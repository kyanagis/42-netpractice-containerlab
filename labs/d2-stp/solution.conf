# stp 模範解答: 全ブリッジで STP を有効化(優先度/タイマは既与)。h1/h2は同一subnet。
sw1: ip link set br0 type bridge stp_state 1
sw2: ip link set br0 type bridge stp_state 1
sw3: ip link set br0 type bridge stp_state 1
h1: ip addr add 10.0.0.1/24 dev eth1
h2: ip addr add 10.0.0.2/24 dev eth1
