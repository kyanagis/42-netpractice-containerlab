# stp 穴埋め。___ を埋めて ./lab.sh apply d2-stp
# 与件: sw1—sw2—sw3 が三角に接続=ループ。STPが無いとブロードキャストストームで通信不能。
#       優先度(sw1=root)とタイマは既与。STP を有効化してループを断つ。
sw1: ip link set br0 type bridge stp_state ___
sw2: ip link set br0 type bridge stp_state ___
sw3: ip link set br0 type bridge stp_state ___
h1: ip addr add 10.0.0.1/24 dev eth1
h2: ip addr add 10.0.0.2/24 dev eth1
